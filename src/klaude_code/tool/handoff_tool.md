Hand off work to a fresh context with compressed history.

Use this tool when:
- The current context is getting too long and response quality is degrading
- You want to continue work with a focused, clean context
- The context window is near capacity

The tool extracts key context from the current conversation and creates a fresh starting point. File system changes are preserved - only conversation history is compressed.

Include a clear goal describing what should happen next in the fresh context.